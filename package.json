{
  "name": "discord-rss",
  "version": "0.1.0",
  "description": "Self-hosted RSS/Atom-to-Discord service for Discord communities. Built entirely in TypeScript ESM with zero runtime dependencies (native http, node:sqlite, built-in web dashboard and API). Posts directly to Discord webhooks.",
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "node": ">=22.9.0"
  },
  "scripts": {
    "build": "tsc",
    "start": "node --env-file-if-exists=.env dist/index.js",
    "dev": "tsc --watch",
    "pretest": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "msw": "^2.15.0",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  },
  "dependencies": {
    "redis": "^5.12.1"
  }
}
