{
  "name": "site-feed-discord",
  "version": "1.0.0",
  "description": "GitHub Actions bot for monitoring feeds and posting to Discord",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.5",
    "@types/node": "^22.0",
    "vitest": "^2.0"
  }
}
