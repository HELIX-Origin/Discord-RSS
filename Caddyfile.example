# Automatic reverse proxy for PUBLIC_URL or custom domain (defaults to localhost)
# Caddy automatically provisions and renews Let's Encrypt / ZeroSSL certificates when a domain is provided:
{$PUBLIC_URL:localhost} {
	reverse_proxy {$INTERNAL_TARGET:127.0.0.1:3131}
}

